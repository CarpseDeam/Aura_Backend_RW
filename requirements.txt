# Web Server & Async
fastapi
uvicorn[standard]
python-dotenv
aiohttp  # <-- NEW: For making requests to our LLM microservice

# Database & Auth
SQLAlchemy
psycopg2-binary
passlib[bcrypt]
python-jose[cryptography]

# AI Core (slimmed down for main app)
openai  # <-- Still needed for embeddings in VectorContextService
chromadb # <-- Still needed for VectorContextService

# Other FastAPI utilities
python-multipart